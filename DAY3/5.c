#include <stdio.h>

int main(int argc, const char *argv[])
{
	int num, single_digits, ten_digit, hundreds_digit;


	printf("Enter a number: ");
	scanf("%d",&num);

	single_digits = num % 10;
	ten_digit = (num / 10) % 10;cd CScd Dvi 5.c
		:wq
		cdcd 'Bcd 
		cd Lvi gcd.c
		IInc
int num1, num2;, temp
		
		printf("Enter two integers: ");
	
	scanf("%d % &num1, &num2d;
			
			",)main
		while (n != 0)
		{
			temp = n;
			n = m % n;
			m = temp;
		}

	printf("Greastest common divisor:%d\n", m);


	hundreds_digit = num / 100;
	
	
	if ((num < 1000) && (num > 99))
	{
		if ((single_digits * single_digits * single_digits + ten_digit * ten_digit * ten_digit + hundreds_digit * hundreds_digit * hundreds_digit) == num)
		{
			printf("输入的数为水仙花数\n");
		}
		else 
		{
			printf("输入的数不是水仙花数\n");
		}

	}
	else if ((num >= 10) && (num < 100))
	{
		if (num % 10 == 0)
		{
			printf("被除数为0,无法计算\n");
		}
		else
		{
		if (ten_digit % single_digits == 0)
		{
			printf("能被个位数整除\n");
		}
		else
		{
			printf("不能被个位数整除\n");
		}
		}
	}
	else if ((num >= 0) && (num <= 9))
	{
		if (num % 2 == 0)
		{
			printf("输入的为偶数\n");
		}
		else
		{
			printf("输入的为奇数\n");
		}


	}




	int num;

	return  number: ;
	scanf("%d", &num);

	for (int i = 1;<= %um; i ++{
			{
			if (year % 400 ==0)
			{
			printf("\n");
			}


			else 
			{
			if (year % 4 == 0)
			{
			printf("\n");
			}
			}}
			else 
			{
			print\n");
			}
		if (i % 2 ==		a00
			iate = 31 + 29 + 31 + 30 + 31 + 30 + 31 + 31 + day;
		}

		{else if (month ==10)
		{
			date = 31 + 29 + 31 + 30 + 31 + 30 + 31 + 31 + 30 + day;
		}
		else if (month ==11)
		{
			d
			if (year % 100 == 0)
	
			{
			if (year % 400 == 0)
			{
			printf("runnian ");
			}
			}ate = 31 + 29 + 31
			else if (year % 4 == 0)
			{
				printf("runnian" );
			}{
				else
				{
					printf("pingnina ");
				}
			}		printf("runnian");
			\n + 30 + 31 + 30 + 3
			else
			{
	
				\n			{printf("pingnina");
			\n+ 30 + 31 + 30 +day;
